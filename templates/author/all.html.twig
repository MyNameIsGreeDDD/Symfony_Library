{% extends 'base.html.twig' %}
{% block title %} {% endblock %}
{% block body %}
    <a href="{{ path('new_author') }}">Добавить автора</a>
    <br>
    {% for author in authors %}
        <a>
            {{ author.name }}
            <a href="{{ path('edit_author', {'id': author.id}) }}">Редактировать</a>
            <a href="{{ path('author_delete', {'id': author.id}) }}">Удалить</a>
        </a>
        <br>
        <hr>


    {% endfor %}
{% endblock %}